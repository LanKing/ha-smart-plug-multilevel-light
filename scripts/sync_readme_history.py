#!/usr/bin/env python3
from __future__ import annotations

import re
import urllib.request
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
PATH = ROOT / "README_RU.md"
POWER_BASELINE = "0d32c707f3d5ec45fa9009a07a98ab7d2cd8964c"
BASE_URL = f"https://raw.githubusercontent.com/LanKing/ha-smart-plug-multilevel-light/{POWER_BASELINE}/README_RU.md"


def main() -> None:
    with urllib.request.urlopen(BASE_URL) as response:
        text = response.read().decode("utf-8")

    text = text.replace("version-0.8.0-blue", "version-0.10.5-blue")
    text = text.replace("Version 0.8.0", "Version 0.10.5")
    text = text.replace("?v=0.8.0", "?v=0.10.5")

    text = text.replace(
        "* Автоматически определяет текущий режим работы светильника по фактически измеренной мощности;",
        "* Раз в секунду фиксирует текущее значение мощности и переключает определённый режим только после нескольких последовательных замеров, соответствующих одному и тому же новому режиму;",
    )
    text = text.replace(
        "* Автоматически предлагает порог режима с запасом относительно измеренной мощности, чтобы небольшие колебания потребления не мешали его определению;",
        "* Помогает измерить стабильную мощность каждого режима: тест завершается после нескольких одинаковых показаний подряд и позволяет сразу применить результат;",
    )
    text = text.replace(
        "* Использует нелинейную шкалу яркости с округлением до 5%, лучше отражающую различия между режимами;",
        "* Рассчитывает яркость линейно относительно максимальной мощности; округление рассчитанной яркости до 5% можно включать для более аккуратного отображения процентов;",
    )
    text = text.replace(
        "* Использует нелинейную шкалу яркости; округление рассчитанной яркости до 5% можно включать и отключать;",
        "* Рассчитывает яркость линейно относительно максимальной мощности; округление рассчитанной яркости до 5% можно включать для более аккуратного отображения процентов;",
    )

    text = text.replace(
        "   - **Power cycle delay** — пауза между отключением и повторной подачей питания; по умолчанию `0.7 s`;",
        "   - **Power cycle delay** — пауза между отключением и повторной подачей питания; по умолчанию `0.7 s`;\n"
        "   - **Consecutive readings to switch** — сколько последовательных секундных замеров должны соответствовать одному новому режиму для переключения; по умолчанию `5`;\n"
        "   - **Round brightness to 5% (may look nicer)** — округление рассчитанной яркости до 5%; по умолчанию выключено;",
    )

    marker = "| **Power cycle delay** | пауза между выключением и повторным включением розетки | `0.7 s` |"
    if marker in text and "| **Consecutive readings to switch**" not in text:
        text = text.replace(
            marker,
            marker + "\n| **Consecutive readings to switch** | сколько последовательных секундных замеров одного нового режима требуется для переключения | `5` |\n"
            "| **Round brightness to 5% (may look nicer)** | округление рассчитанной яркости до ближайших 5%; может сделать отображение процентов аккуратнее | выключено |",
        )

    text = text.replace(
        "6. Для каждого режима переключите лампу её физической кнопкой, посмотрите текущее значение датчика мощности и внесите его в **Brightness modes**. Например:",
        "6. Для каждого режима переключите лампу её физической кнопкой, откройте редактор режима и запустите **Test stable power**. После получения стабильного значения нажмите **Apply**. Например:",
    )

    text = re.sub(
        r"### Как подобрать пороги режимов\n\n.*?(?=\nПример:\n)",
        "### Как подобрать пороги режимов\n\n1. Включите лампу в нужный режим физической кнопкой.\n2. Добавьте или откройте соответствующий режим в **Brightness modes**.\n3. Нажмите **Test stable power**. Во время теста отображается текущая последовательность одинаковых секундных показаний.\n4. Когда будет получено настроенное число одинаковых значений подряд, нажмите **Apply** — мощность подставится в поле, а модальное окно сохранится, если остальные поля валидны.\n5. Повторите для остальных режимов.\n\nСправа от поля мощности также отображается **Current moment power**, а в строке с кнопкой **Add** — постоянно обновляемый стек `🐞 Last measures: [a, b, c, ...] W`. В списке режимов рядом с мощностью показывается рассчитанный процент яркости, например `2 W · 33%`.\n",
        text,
        flags=re.DOTALL,
    )

    text = re.sub(
        r"Режим выбирается по правилу: используется максимальный настроенный порог, который меньше или равен измеренной мощности\..*?(?=\n## 🔌 Как определяется состояние)",
        "После включения питания интеграция сразу предполагает самый высокий настроенный режим. Это предотвращает кратковременное отображение старого режима, пока датчик мощности ещё не успел обновиться. Затем текущее значение датчика мощности фиксируется раз в секунду, а изменения состояния датчика обрабатываются сразу. Каждый замер сопоставляется с пороговой логикой: используется максимальный настроенный порог, который меньше или равен этому значению. Интеграция переключает предварительно выбранный высокий режим на другой только тогда, когда последние **N** последовательных замеров все соответствуют одному и тому же новому режиму. **N** задаётся параметром **Consecutive readings to switch** и по умолчанию равно `5`. При нулевой мощности лампа остаётся выключенной, но следующий переход к положительной мощности снова начинается с самого высокого режима до подтверждения измерениями.\n\nВ редакторе режима есть тест стабильной мощности. После запуска он показывает текущую непрерывную последовательность одинаковых показаний, например `Testing, wait [5, 5, 5]`. При изменении значения последовательность начинается заново. Тест завершается после получения **N** одинаковых значений подряд. Результат можно применить кнопкой **Apply**: значение подставляется в поле мощности, после чего модальное окно сохраняется, если его валидатор проходит.\n\nВ строке с кнопкой **Add** справа постоянно отображается стек последних секундных измерений мощности в формате `🐞 Last measures: [a, b, c, ...] W`. Размер стека соответствует значению **Consecutive readings to switch**. В списке режимов мощность каждого пресета дополнена рассчитанным процентом яркости; процент использует ту же линейную формулу и настройку округления, что и сущность.\n\nЯркость режима рассчитывается линейно относительно максимального настроенного порога мощности: `brightness = power / max_power × 100`. Например, при максимуме `6 W` режимы `1 / 3 / 4 / 6 W` соответствуют примерно `17 / 50 / 67 / 100%`. Опциональное округление до 5% по умолчанию выключено.\n",
        text,
        flags=re.DOTALL,
    )

    text = re.sub(
        r"## 🎚 Условный процент яркости\n\n.*?(?=\n## 🕹 Управление лампой)",
        "## 🎚 Условный процент яркости\n\nПроцент рассчитывается линейно по относительной мощности настроенного режима: `brightness = power / max_power × 100`. Максимальный настроенный режим получает `100%`, остальные — пропорционально своей мощности. Этот же процент показывается рядом с мощностью пресета в конфигураторе.\n\nНапример, при порогах `2 W`, `3 W`, `4 W` и `6 W` получаются примерно `33%`, `50%`, `67%` и `100%`. Опция **Round brightness to 5% (may look nicer)** может округлять результат до ближайших `5%`, но по умолчанию она выключена. Процент используется только для отображения режима в карточке и атрибутах сущности; физическая лампа не получает команду установки яркости.\n",
        text,
        flags=re.DOTALL,
    )

    text = re.sub(
        r"### Определяется неправильный режим\n\n.*?(?=\n### )",
        "### Определяется неправильный режим\n\nПроверьте `🐞 Last measures` в настройках и атрибуты `power_history`, `power_history_modes`, `power_sample_interval_seconds` и `selected_power_mode`. `power_history` показывает последние зафиксированные значения мощности, `power_history_modes` — режим для каждого из них по настроенным порогам, `power_sample_interval_seconds` — интервал периодической фиксации, а `selected_power_mode` — текущий подтверждённый режим.\n",
        text,
        flags=re.DOTALL,
    )

    PATH.write_text(text, encoding="utf-8")


if __name__ == "__main__":
    main()
