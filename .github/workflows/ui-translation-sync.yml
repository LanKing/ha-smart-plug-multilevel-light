name: UI translation sync

on:
  push:
    paths:
      - ".github/workflows/ui-translation-sync.yml"
      - "scripts/sync_new_settings_translations.py"
      - "scripts/sync_translations.py"
      - "custom_components/smart_plug_multilevel_light/strings.json"
      - "custom_components/smart_plug_multilevel_light/translations/**"
      - "custom_components/smart_plug_multilevel_light/static/smart-plug-multilevel-light-locales.js"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Restore the complete UI translation baseline
        run: python scripts/sync_new_settings_translations.py
      - name: Apply localized UI text
        run: python scripts/sync_translations.py
      - name: Commit synchronized files
        run: |
          if git diff --quiet; then
            echo "UI translations are already synchronized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add custom_components/smart_plug_multilevel_light/translations custom_components/smart_plug_multilevel_light/static/smart-plug-multilevel-light-locales.js
          git commit -m "Synchronize UI translations"
          git pull --rebase
          git push
