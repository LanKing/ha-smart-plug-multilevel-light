name: Remove README image break

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/remove-readme-image-break.yml"

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Remove br after modes-edit image
        run: |
          python <<'PY'
          from pathlib import Path

          paths = [Path("README.md"), *sorted(Path("docs").glob("README_*.md"))]
          old = '<img src="docs/modes-edit.png" width="465"/><br />'
          new = '<img src="docs/modes-edit.png" width="465"/>'

          changed = []
          for path in paths:
              text = path.read_text(encoding="utf-8")
              updated = text.replace(old, new)
              if updated != text:
                  path.write_text(updated, encoding="utf-8")
                  changed.append(str(path))

          print(f"Changed {len(changed)} README files")
          for path in changed:
              print(path)
          PY

      - name: Commit README changes and remove one-off workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/remove-readme-image-break.yml
          git add README.md docs/README_*.md
          git commit -m "Remove line break after README mode image"
          git pull --rebase origin main
          git push
