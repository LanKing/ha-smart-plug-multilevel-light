{
  "title": "Smart Plug Multi-Level Light",
  "config": {
    "step": {
      "user": {
        "title": "Select a smart plug",
        "description": "Only switches whose Home Assistant device exposes a current sensor are shown.",
        "data": {
          "outlet": "Smart plug"
        }
      },
      "settings": {
        "title": "Configure light and modes",
        "data": {
          "name": "Light name",
          "current_sensor": "Current sensor",
          "power_cycle_delay": "Power-cycle delay",
          "current_history_samples": "Recent current readings",
          "round_brightness_to_5": "Round brightness to 5%",
          "modes": "Brightness modes"
        },
        "data_description": {
          "current_sensor": "Only current sensors belonging to the selected smart plug are available here.",
          "power_cycle_delay": "Usually no additional delay is needed. On some plugs, however, the power-off interval may be too short for the lamp to fully lose power. If a lamp that was switched off with its own button does not turn on from Home Assistant, set a small delay here, for example 0.7 s.",
          "current_history_samples": "Number of latest current readings used to determine the mode. Readings that do not exactly match a configured mode value are ignored; among the remaining readings, the most frequent value wins. Default is 5.",
          "round_brightness_to_5": "Rounds calculated brightness to the nearest 5%. Disable this to use whole percentages without 5% steps.",
          "modes": "For a new lamp, switch it through each brightness level with its physical button and add the measured current value for every mode, for example High, Medium, Low, and Dim. Only exact configured current values participate in mode detection."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    },
    "abort": {
      "current_sensor_missing": "No current sensor was found for the selected plug"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure multi-level light",
        "data": {
          "name": "Light name",
          "current_sensor": "Current sensor",
          "power_cycle_delay": "Power-cycle delay",
          "current_history_samples": "Recent current readings",
          "round_brightness_to_5": "Round brightness to 5%",
          "modes": "Brightness modes"
        },
        "data_description": {
          "current_sensor": "Only current sensors belonging to the selected smart plug are available here.",
          "power_cycle_delay": "Usually no additional delay is needed. On some plugs, however, the power-off interval may be too short for the lamp to fully lose power. If a lamp that was switched off with its own button does not turn on from Home Assistant, set a small delay here, for example 0.7 s.",
          "current_history_samples": "Number of latest current readings used to determine the mode. Readings that do not exactly match a configured mode value are ignored; among the remaining readings, the most frequent value wins. Default is 5.",
          "round_brightness_to_5": "Rounds calculated brightness to the nearest 5%. Disable this to use whole percentages without 5% steps.",
          "modes": "For a new lamp, switch it through each brightness level with its physical button and add the measured current value for every mode, for example High, Medium, Low, and Dim. Only exact configured current values participate in mode detection."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    }
  }
}
