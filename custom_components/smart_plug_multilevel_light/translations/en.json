{
  "title": "Smart Plug Multi-Level Light",
  "config": {
    "step": {
      "user": {
        "title": "Select a smart plug",
        "description": "Only switches whose Home Assistant device exposes a power sensor are shown.",
        "data": {
          "outlet": "Smart plug"
        }
      },
      "settings": {
        "title": "Configure light and modes",
        "data": {
          "name": "Light name",
          "power_cycle_delay": "Power-cycle delay",
          "modes": "Brightness modes",
          "power_sensor": "Power sensor",
          "round_brightness_to_5": "Round brightness to 5%",
          "power_history_samples": "Consecutive readings to switch"
        },
        "data_description": {
          "power_cycle_delay": "Usually no additional delay is needed. If a lamp switched off with its own button does not turn on from Home Assistant, set a small delay, for example 0.7 s.",
          "modes": "For a new lamp, first teach the integration its brightness modes. Use the physical button on the lamp to select each brightness level and add it below, for example High, Medium, Low, and Dim.",
          "power_sensor": "Only power sensors belonging to the selected smart plug are available here.",
          "round_brightness_to_5": "Rounds calculated brightness to the nearest 5%. Disable to use whole percentages without 5% steps.",
          "power_history_samples": "Number of consecutive power readings that must map to the same new mode before the integration switches modes. Until then, the current mode is kept unchanged. Default is 3."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    },
    "abort": {
      "power_sensor_missing": "No power sensor was found for the selected plug"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure multi-level light",
        "data": {
          "name": "Light name",
          "power_cycle_delay": "Power-cycle delay",
          "modes": "Brightness modes",
          "power_sensor": "Power sensor",
          "round_brightness_to_5": "Round brightness to 5%",
          "power_history_samples": "Consecutive readings to switch"
        },
        "data_description": {
          "power_cycle_delay": "Usually no additional delay is needed. If a lamp switched off with its own button does not turn on from Home Assistant, set a small delay, for example 0.7 s.",
          "modes": "For a new lamp, first teach the integration its brightness modes. Use the physical button on the lamp to select each brightness level and add it below, for example High, Medium, Low, and Dim.",
          "power_sensor": "Only power sensors belonging to the selected smart plug are available here.",
          "round_brightness_to_5": "Rounds calculated brightness to the nearest 5%. Disable to use whole percentages without 5% steps.",
          "power_history_samples": "Number of consecutive power readings that must map to the same new mode before the integration switches modes. Until then, the current mode is kept unchanged. Default is 3."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    }
  }
}
