{
  "title": "Smart Plug Multi-Level Light",
  "config": {
    "step": {
      "user": {
        "title": "Select a smart plug",
        "description": "Only switches whose Home Assistant device exposes both power and current sensors are shown.",
        "data": {
          "outlet": "Smart plug"
        }
      },
      "settings": {
        "title": "Configure light and modes",
        "description": "Sensors are limited to the selected smart plug.",
        "data": {
          "name": "Light name",
          "power_sensor": "Power sensor",
          "current_sensor": "Current sensor",
          "power_threshold": "Power threshold",
          "power_cycle_delay": "Power-cycle delay",
          "modes": "Brightness modes"
        },
        "data_description": {
          "power_threshold": "Power at or below the threshold is treated as OFF; usually leave it at 0 W.",
          "modes": "All modes are edited on one page and automatically sorted by current."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    },
    "abort": {
      "metering_entities_missing": "No power and current sensors were found for the selected plug"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure multi-level light",
        "data": {
          "name": "Light name",
          "power_sensor": "Power sensor",
          "current_sensor": "Current sensor",
          "power_threshold": "Power threshold",
          "power_cycle_delay": "Power-cycle delay",
          "modes": "Brightness modes"
        },
        "data_description": {
          "power_threshold": "Power at or below the threshold is treated as OFF; usually leave it at 0 W.",
          "modes": "All modes are edited on one page and automatically sorted by current."
        }
      }
    },
    "error": {
      "at_least_one_mode": "Add at least one mode"
    }
  }
}
